# Frontend Environment Variables for Clear Clause AI
# Copy this file to .env.local and adjust values for your environment

# ============================================
# Backend API Configuration
# ============================================

# Backend API base URL (FastAPI server)
# Local development: http://localhost:8000
# Production: https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:8000

# API timeout in milliseconds (increased for document processing operations)
# Default: 60000ms (60 seconds) for large document handling
# Minimum: 30000ms recommended
VITE_API_TIMEOUT=60000

# ============================================
# Development Settings (optional)
# ============================================

# Enable detailed API request/response logging in browser console
# Use 'true' for development, 'false' or omit for production
VITE_ENABLE_API_LOGGING=true

# Client-side maximum upload file size in MB
# Must match or be less than backend MAX_FILE_SIZE_MB
VITE_MAX_UPLOAD_SIZE_MB=10

# ============================================
# Important Notes
# ============================================

# Frontend runs on port 8080 (Vite dev server by default)
# Backend runs on port 8000 (FastAPI server)
# Ensure backend CORS_ORIGINS includes http://localhost:8080

# CORS Issue? Update backend .env:
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Production Deployment:
# 1. Set VITE_API_BASE_URL to your deployed backend URL
# 2. Set VITE_ENABLE_API_LOGGING=false
# 3. Update VITE_API_TIMEOUT based on your network conditions
# 4. Ensure backend CORS includes your production domain
